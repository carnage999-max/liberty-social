# Stripe Publishable Key (required for PaymentSheet)
# Get this from: https://dashboard.stripe.com/apikeys (use test key for development)
# Must be prefixed with EXPO_PUBLIC_ to be available in the app
EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxx

# API URL (optional, defaults to https://api.mylibertysocial.com/api)
EXPO_PUBLIC_API_URL=http://localhost:8000/api

# Backend URL - used for deriving WebSocket endpoint
# Examples:
#   For production: https://api.mylibertysocial.com (will use notifications.mylibertysocial.com for WS)
#   For local dev: http://192.168.1.100:8000 (your machine's LAN IP)
EXPO_PUBLIC_BACKEND_URL=http://localhost:8000

# WebSocket URL (optional - if not set, derived from EXPO_PUBLIC_BACKEND_URL)
# Explicit configuration if automatic derivation doesn't work:
#   EXPO_PUBLIC_BACKEND_WS_URL=wss://notifications.mylibertysocial.com/ws/notifications/
#   EXPO_PUBLIC_BACKEND_WS_URL=ws://localhost:8000/ws/notifications/